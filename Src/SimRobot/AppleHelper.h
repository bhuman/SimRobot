/**
 * @file SimRobot/AppleHelper.cpp
 * This file declares a function that sets the window background color
 * and makes the title bar drawable using Objective C++ on macOS.
 * @author Thomas Röfer
 */

#pragma once

/**
 * Sets the window background color to the official one and makes
 * the title bar drawable.
 * @param window The main window.
 */
void fixMainWindow(WId window);
